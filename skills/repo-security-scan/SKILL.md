# repo-security-scan (DRAFT)

## What it does
Scans a target repo for accidental credential leaks and common insecure patterns, then produces a PR-ready report.

## Safety
- Default mode is read-only.
- Does not exfiltrate secrets.

## Usage
(TODO)
